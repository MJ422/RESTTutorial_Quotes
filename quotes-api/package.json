{
  "name": "quotes-api",
  "version": "1.0.0",
  "description": "Small sample-case of a REST API built to work with an array of quotes, returning and modifying them as JSON objects. ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike C",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.16.4"
  }
}
